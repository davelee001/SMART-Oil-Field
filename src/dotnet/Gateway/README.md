# .NET Gateway (placeholder)

dotnet SDK not detected. Install from https://dotnet.microsoft.com/download to scaffold:

Commands:
- dotnet new worker -n Gateway -o src/dotnet/Gateway
- dotnet build src/dotnet/Gateway
- dotnet run --project src/dotnet/Gateway
